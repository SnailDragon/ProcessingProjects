////needs to be commented out after use testing
//float[][] dots = new float[100][2];

//void setup(){
//  size(960,520);
//  background(0);
//  noStroke();
  
//  for(int i = 0; i < dots.length; i++){
//    dots[i][0] = random(width);
//    dots[i][1] = height/2; //random(height/4);
//  }
  
//  for(int i = 0; i < dots.length; i++){
//    circle(dots[i][0], dots[i][1], 5);
//  }
  
//  for(int i = 0; i < dots.length; i++){
//    float midpoint = width/2;
//    dots[i][0] = dots[i][0] - (dots[i][0] / pow((dots[i][0] - midpoint) * 2, 2)); // (x-midpoint)(x+midpoint) + midpoint^2
//    println(dots[i][0]);
//  }
  
//  for(int i = 0; i < dots.length; i++){
//    circle(dots[i][0], dots[i][1]+100, 5);
//  }
  
//}

//void draw(){
  
  
  
//}
